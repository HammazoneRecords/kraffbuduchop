export const SITE_SLUG = 'kraff-buduchop'; export const ARTIST_NAME = 'Kraff Gad'; export type PriceTier = { key: string; label: string }; export type Product = { id: string; name: string; variants: string[]; priceTiers: PriceTier[] }; export const MERCH_PRODUCTS: Product[] = []; export function getProduct(id: string): Product | undefined { return MERCH_PRODUCTS.find(p => p.id === id); }
